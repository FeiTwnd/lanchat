

---

# 提示词：Java 课程设计——网络聊天程序（题目13）

## 一、任务身份

你是一名精通 Java SE 的软件工程师，正在为高校《Java 课程设计》完成一个**冲高分**的项目。项目题目是**题目13：网络聊天程序**。你必须独立完成从架构设计、编码实现、测试到文档的全过程，代码必须是原创，不得抄袭网络或往届代码。

---

## 二、项目目标

实现一个类似 QQ 的**局域网聊天程序**，采用 **C/S 架构 + GUI 界面 + 多线程 + Socket 网络编程**，并满足以下核心功能：

1. **自动侦测局域网内部启动该软件的用户**，并显示在用户列表中。
2. **两个人之间的私聊**。
3. **群聊**。
4. **发送文件**。

同时必须满足课程总体要求：
- 有效代码不少于 **500 行**（实际目标 1500+ 行），注释规范、适量。
- 合理运用**封装、继承、多态、抽象类、接口**。
- 实现**数据持久化**（推荐文件 + 可选数据库）。
- 使用 **GUI** 交互（Swing）。
- 正确运用**集合、泛型、多态、异常**。
- **多线程 + 网络编程**是加分项，必须做。
- **不使用任何现成开发框架**（禁止 Netty、Spring 等），只用 Java SE 标准库。
- 能清楚表达工作，使用专业术语和图形（类图、时序图、架构图）。

---

## 三、系统架构设计（必须实现）

采用分层架构，模块间接口清晰：

```
com.chat
├── common/          公共模块
│   ├── Message.java          消息实体（泛型/序列化）
│   ├── MessageType.java      消息类型枚举
│   ├── User.java             用户实体
│   └── Constants.java        常量（端口、协议头等）
├── server/          服务器端
│   ├── ChatServer.java       服务器主类
│   ├── ClientHandler.java    客户端处理器（多线程）
│   ├── UserManager.java      在线用户管理（ConcurrentHashMap）
│   └── ServerUI.java         服务器 GUI（可选）
├── client/          客户端
│   ├── ChatClient.java       客户端主类
│   ├── ClientUI.java         客户端 GUI 主窗口
│   ├── LoginUI.java          登录窗口
│   ├── PrivateChatUI.java    私聊窗口
│   ├── GroupChatUI.java      群聊窗口
│   └── FileTransferUI.java   文件传输窗口
├── dao/             持久化层
│   ├── BaseDao.java          泛型接口
│   ├── UserDao.java          用户 DAO 接口
│   ├── UserDaoImpl.java      文件/数据库实现
│   ├── MessageDao.java       消息记录 DAO
│   └── MessageDaoImpl.java
├── service/         业务层
│   ├── UserService.java
│   ├── MessageService.java
│   └── FileService.java
├── exception/       自定义异常
│   ├── ChatException.java
│   ├── UserNotFoundException.java
│   └── FileTransferException.java
├── util/            工具类
│   ├── JsonUtil.java         消息序列化（可用简单分隔符）
│   ├── FileUtil.java
│   ├── DateUtil.java
│   └── SecurityUtil.java     密码哈希
└── test/            单元测试
    ├── UserServiceTest.java
    ├── MessageServiceTest.java
    └── ChatClientTest.java
```

---

## 四、功能模块详细要求

### 模块1：局域网自动发现
- 使用 **UDP 广播** 实现：客户端启动后向局域网广播 `DISCOVER` 消息，服务器或其它客户端回应，从而发现同一局域网内在线用户。
- 或者：客户端启动后连接服务器，服务器维护在线用户列表，并推送给所有客户端。
- 用户列表必须实时更新：用户上线、下线、改名都要刷新。
- 使用 `ConcurrentHashMap<String, User>` 管理在线用户，保证线程安全。

### 模块2：私聊
- 客户端 A 选择用户 B，发送私聊消息。
- 消息格式包含：发送者、接收者、内容、时间、类型。
- 服务器根据接收者找到对应 `ClientHandler`，转发消息。
- 接收方 GUI 弹出私聊窗口并显示消息。

### 模块3：群聊
- 客户端发送群聊消息，服务器广播给所有在线用户。
- 群聊窗口显示所有消息，包含发送者昵称、时间、内容。
- 支持系统通知（如“XXX 上线了”）。

### 模块4：文件传输
- 客户端选择文件，发送文件传输请求。
- 接收方确认后，通过 Socket 传输文件字节流。
- 支持进度条显示（`JProgressBar`）。
- 文件保存到本地指定目录，并记录传输日志。
- 使用多线程处理文件传输，避免阻塞 UI。

### 模块5：用户管理（增删查改）
- 用户注册：用户名、密码、昵称。
- 用户登录：验证密码（密码用 SHA-256 哈希存储，不存明文）。
- 用户信息修改：昵称、密码。
- 用户查询：按用户名查询。
- 用户删除：管理员可删除用户。
- 持久化：用户信息保存到 `users.txt` 或数据库（推荐 SQLite/MySQL）。

### 模块6：消息持久化
- 聊天记录保存到文件或数据库。
- 支持按用户、时间范围查询历史消息。
- 支持导出聊天记录。

---

## 五、技术点强制覆盖

| 技术点 | 要求 |
|---|---|
| 封装 | 实体类私有字段 + getter/setter |
| 继承 | `BaseUI`、`BaseDao`、`BaseMessage` |
| 多态 | `Message` 抽象类，`TextMessage`、`FileMessage`、`SystemMessage` 子类 |
| 抽象类 | `AbstractMessageHandler` |
| 接口 | `MessageHandler`、`BaseDao<T>`、`UserService` |
| 泛型 | `BaseDao<T>`、`Result<T>` |
| 集合 | `ConcurrentHashMap`、`CopyOnWriteArrayList`、`List`、`Map` |
| 异常 | 自定义异常 + try-catch-finally + try-with-resources |
| 多线程 | 线程池 `ExecutorService`、`ClientHandler` 多线程、文件传输线程 |
| 网络 | `ServerSocket`、`Socket`、`DatagramSocket`（UDP 广播） |
| GUI | Swing：`JFrame`、`JPanel`、`JTable`、`JList`、`JTextArea`、`JProgressBar` |
| 持久化 | 文件 IO + 可选 JDBC |
| 设计模式 | 单例（服务器）、工厂（消息创建）、策略（消息处理）、DAO |
| 序列化 | `Serializable` 或自定义协议 |
| 日志 | `java.util.logging` 记录关键操作 |

---

## 六、代码规范要求

1. 包名小写，类名大驼峰，方法名小驼峰，常量全大写。
2. 每个类必须有类注释，说明职责、作者、版本。
3. 每个方法必须有方法注释，说明参数、返回值、异常。
4. 关键逻辑必须有行内注释。
5. 注释必须是有效代码的一部分，不能敷衍。
6. 禁止硬编码：端口、路径、密码等放到 `Constants` 或配置文件。
7. 异常必须处理，不能空 catch。
8. 资源必须关闭（try-with-resources）。
9. 代码结构清晰，方法不宜过长，职责单一。

---

## 七、测试要求（对应评分点7）

必须编写 **单元测试 + 集成测试**：

### 单元测试（JUnit 或 main 方法模拟）
- `UserServiceTest`：注册、登录、密码哈希、重复用户名。
- `MessageServiceTest`：消息创建、序列化、反序列化。
- `FileServiceTest`：文件分块、传输、校验。
- `UserDaoTest`：增删查改、持久化。

### 集成测试
- 启动服务器 + 2 个客户端，测试私聊。
- 测试群聊广播。
- 测试文件传输。
- 测试用户上线/下线列表刷新。
- 测试异常场景：服务器未启动、用户不存在、文件过大。

### 测试报告
- 测试用例表（编号、输入、预期、实际、结果）。
- 测试截图或日志。
- 缺陷记录与修复说明。

---

## 八、文档要求（对应评分点8、9、10）

必须提供以下文档：

1. **需求分析**：功能需求、非功能需求、用例图。
2. **系统设计**：
   - 架构图（C/S 分层）
   - 类图（UML）
   - 时序图（登录、私聊、群聊、文件传输）
   - 数据库 ER 图（如果使用数据库）
   - 模块接口说明
3. **实现说明**：关键类、关键算法、技术难点。
4. **测试报告**：单元测试、集成测试、测试结果。
5. **用户手册**：如何启动服务器、客户端、使用各功能。
6. **课程设计报告**：按学校模板，内容详实、图表规范、逻辑清晰。
7. **PPT**：用于答辩，包含架构、功能演示、技术亮点、难点解决。

---

## 九、开发步骤（必须按顺序执行）

### 第一步：总体架构设计
- 确定 C/S 架构、模块划分、类间关系。
- 画出架构图和类图。
- 定义公共实体、接口、异常。
- 搭建项目包结构。

### 第二步：模块设计与实现
按以下顺序实现，每完成一个模块就测试：

1. **公共模块**：`Message`、`User`、`MessageType`、`Constants`。
2. **持久化层**：`BaseDao<T>`、`UserDaoImpl`、`MessageDaoImpl`。
3. **业务层**：`UserService`、`MessageService`、`FileService`。
4. **服务器端**：`ChatServer`、`ClientHandler`、`UserManager`。
5. **客户端网络层**：`ChatClient`、消息收发线程。
6. **客户端 GUI**：`LoginUI`、`ClientUI`、`PrivateChatUI`、`GroupChatUI`、`FileTransferUI`。
7. **局域网发现**：UDP 广播或服务器推送。
8. **文件传输**：分块传输 + 进度条。
9. **异常处理**：统一异常体系。
10. **日志**：关键操作记录。

### 第三步：测试与评价
- 编写单元测试。
- 进行集成测试。
- 记录测试结果。
- 评价系统性能、稳定性、可扩展性。
- 撰写测试报告。

---

## 十、交付物清单

智能体必须最终输出：

1. **完整可运行的 Java 项目源码**（按包结构组织）。
2. **所有类、接口、抽象类、异常类**。
3. **GUI 界面代码**。
4. **单元测试与集成测试代码**。
5. **配置文件**（端口、路径等）。
6. **用户数据文件/数据库脚本**。
7. **README.md**：项目说明、启动方式、功能列表。
8. **设计文档**：架构图、类图、时序图、ER 图。
9. **测试报告**。
10. **课程设计报告草稿**。
11. **答辩 PPT 大纲**。

---

## 十一、加分项（必须尽量实现）

- ✅ 多线程：线程池、文件传输线程、消息处理线程。
- ✅ 网络编程：TCP + UDP。
- ✅ 数据库：SQLite 或 MySQL + JDBC。
- ✅ 密码安全：SHA-256 哈希。
- ✅ 设计模式：单例、工厂、策略、DAO。
- ✅ 日志系统。
- ✅ 异常体系。
- ✅ 单元测试 + 集成测试。
- ✅ GUI 友好：登录、主窗口、私聊、群聊、文件传输、用户列表。
- ✅ 聊天记录持久化与查询。
- ✅ 文件传输进度条。
- ✅ 用户上线/下线通知。
- ✅ 心跳检测（可选）。
- ✅ 消息加密（可选，XOR/AES）。

---

## 十二、禁止事项

1. 禁止使用 Netty、Spring、JavaFX 等框架（Swing 可用）。
2. 禁止抄袭网络或往届代码。
3. 禁止空 catch、硬编码、无注释。
4. 禁止只做控制台不做 GUI（GUI 是评分点）。
5. 禁止跳过测试和文档。
6. 禁止功能残缺：私聊、群聊、文件传输、用户列表必须全部实现。

---

## 十三、输出格式要求

请按以下顺序输出：

1. **项目结构说明**（包结构 + 类职责）。
2. **核心代码**（按模块分文件输出，每个文件完整可编译）。
3. **测试代码**。
4. **配置文件**。
5. **README**。
6. **设计文档**（架构图、类图、时序图用文字或 Mermaid 描述）。
7. **测试报告**。
8. **课程设计报告草稿**。
9. **PPT 大纲**。

每个 Java 文件必须包含：
- 包声明
- import
- 类注释
- 完整实现
- 规范注释

---

## 十四、最终目标

交付一个**功能完整、架构清晰、代码规范、测试充分、文档齐全**的局域网聊天程序，能够满足课程设计所有评分点，并具备多线程、网络编程、数据库、GUI、设计模式等加分项，达到**冲高分**的目标。
